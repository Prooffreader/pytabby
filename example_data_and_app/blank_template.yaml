%YAML 1.2
---
case_sensitive:
screen_width:
tabs:
  - header_choice_displayed_and_accepted:
    header_description:
    long_description: 
    items:
      - choice_displayed:
        choice_description:
        valid_entries: 
          - 
        returns: 

# # above, but with every line commented with explanation
#
# case_sensitive:  # optional, boolean, default false
# screen_width:  # optional, integer, default 80
# tabs:
#   - header_choice_displayed_and_accepted:  # required, will be changed to str if not already
#     header_description: File  # optional, default None, will be changed to str if present and not already
#     long_description: Changing to File tab  # optional, default None, will be changed to str if present and not already
#     items:
#       - choice_displayed:  # required, will be changed to str if not already
#         choice_description:  # required, will be changed to str if not already
#         valid_entries:
#           -   # can have multiple entries, will be changed to str if not already
#         returns:  # will be changed to str if not already