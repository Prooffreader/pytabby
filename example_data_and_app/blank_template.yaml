%YAML 1.2
---
case_sensitive:
screen_width:
tabs:
  - header_entry:
    header_description:
    long_description: 
    items:
      - choice_displayed:
        choice_description:
        valid_entries: 
          - 
        returns: 

# # above, but with every line commented with explanation
#
# case_sensitive:  # optional, boolean, default false
# screen_width:  # optional, integer, default 80
# tabs:
#   - header_entry:  # required, will be changed to str if not already str
#     header_description: File  # optional, default None, will be changed to str if present, not None and not already str
#     long_description: Changing to File tab  # optional, default None, will be changed to str if present, not None and not already str
#     items:
#       - choice_displayed:  # required, will be changed to str if not already str
#         choice_description:  # required, will be changed to str if not already str
#         valid_entries:
#           -   # list/array, can have multiple entries, each will be changed to str if not already str
#         returns:  # will be changed to str if not already str